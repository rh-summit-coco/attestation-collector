apiVersion: v1
kind: Service
metadata:
  name: attestation-collector
  labels:
    app: attestation-collector
spec:
  selector:
    app: attestation-collector
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
